{# -- Component Variables -- #}

{% set this = {
    video: params.section|default(null)
} %}

{# -- Component Template -- #}

{% if this.video %}
<div class="c-video">

    {% include '_elements/video.twig' with {
        params: {
            video: this.video.video_url
        }
    } only %}

</div>
{% endif %}
