{# -- Component Variables -- #}

{% set this = {
    embedSection: params.section|default(null)
} %}

{# -- Component Template -- #}

{% if this.embedSection %}

    {% include '_elements/embed.twig' with {
        params: {
            src: this.embedSection.embed_code
        },
    } only %}

{% endif %}
