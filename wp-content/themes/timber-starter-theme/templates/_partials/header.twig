{# -- Partial Variables -- #}
{# ----------------------------------------------------------------- #}

{% set this = {
    primary: params.menu|default(null),
    utility: params.utility|default(null)
} %}

{# -- Partial Template -- #}
{# ----------------------------------------------------------------- #}

{% if this.utility %}
    {% include '_components/nav-utility.twig' with {
        utilityMenu: this.utility
    } %}
{% endif %}

{% if this.primary %}
    {% include '_components/nav-primary.twig' with {
        navMenu: this.primary
    } %}
{% endif %}
