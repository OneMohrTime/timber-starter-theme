<div class="testimonial-block">
    {# <blockquote>{{ fields.quote }}</blockquote> #}
    {# <p class="author">{{ fields.author }}</p> #}
    <pre>
        {# {{ dump(attributes) }} #}
        {{ dump(fields) }}
        {# {{ dump(is_preview) }} #}
    </pre>
</div>
